#pragma once

#include <iostream>
#include <fstream>
#include <string>
#include <vector>

using std::cin;
using std::cout;
using std::endl;

using std::fstream;
using std::string;
using std::vector;

//#include "pockets.hpp"

class GameBoard
{
public:
	GameBoard();

	~GameBoard();

private:

};

